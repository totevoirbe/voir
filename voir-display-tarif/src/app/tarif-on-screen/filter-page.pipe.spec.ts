import { FilterPagePipe } from './filter-page.pipe';

describe('FilterPagePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPagePipe();
    expect(pipe).toBeTruthy();
  });
});
